{
  "name": "@what-does-the-dude-say/interfaces",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@preact/signals": "^2.0.4"
  },
  "scripts": {
    "prebuild": "rm -rf *.tsbuildinfo dist",
    "build": "tsc -p tsconfig-build.json",
    "test": "vitest . --run",
    "lint": "eslint ."
  },
  "private": true
}
